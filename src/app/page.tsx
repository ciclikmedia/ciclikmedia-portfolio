import Reveal from '@/components/Reveal';

export default function HomePage() {
  return (
    <main>
      <section className="section">
        <div className="container">
          <Reveal>
            <h1>Ciclikmedia Portfolio</h1>
          </Reveal>

          <Reveal>
            <p>
              Motion architecture configured successfully.
            </p>
          </Reveal>
        </div>
      </section>
    </main>
  );
}